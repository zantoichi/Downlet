## 1. P0 G0 Direction And Source Reconciliation

- [x] 1.1 Scope: reconcile live product/design requirements with state-driven sizing before any code changes; Out of scope: rewriting historical G0–G2 screenshots, evidence, or accepted observations; Expected files/components: `PRODUCT.md`, `DESIGN.md`, and only conflicting current clauses in `openspec/changes/design-primary-download-window/{proposal.md,design.md,specs/primary-download-flow/spec.md,tasks.md}`; Acceptance: fixed-height/fixed-geometry requirements are replaced by the compact/expanded contract, this change is marked P0 ahead of remaining G3 work, completed historical claims remain intact, and both changes validate; IntelliJ MCP: inspect all edited planning files and confirm no unrelated requirement drift; Compose MCP: not required; Evidence: changed-clause inventory, validation output, and exact commit.
- [x] 1.2 Scope: calibrate the G0 adaptive-window direction from current Jewel metrics and Windows work-area constraints; Out of scope: production implementation, new visual language, width animation, or backend work; Expected files/components: `docs/design/reviews/state-driven-window-sizing/G0-direction/` with preferred width, compact/expanded preferred and minimum heights, outer/client bounds, state-tier matrix, motion timings, manual-ownership examples, constrained-screen fallback, and light/dark sketches or captures; Acceptance: dimensions are justified by visible content rather than prior constants, Empty/Resolving contain no reserved work-plane space, Ready and later states fit normally without scrolling, and 100%/125%/150% scaling cases are addressed; IntelliJ MCP: inspect current layout constants/components and build unchanged code; Compose MCP: measure current Empty/Resolving/Ready content and capture only the evidence needed to calibrate targets; Evidence: review artifact, measurements, captures, and exact commit.
- [x] 1.3 Scope: package and present the P0 G0 direction, then stop; Out of scope: implementation or dispatching implementation work before approval; Expected files/components: G0 `REVIEW.md`, `EVIDENCE.md`, and `manifest.json`; Acceptance: package states strengths, trade-offs, exact proposed profiles, standards sources, inspection points, and ends `AWAITING USER: APPROVE G0 or REVISE G0: <feedback>`; IntelliJ MCP: verify package paths and references; Compose MCP: confirm referenced calibration captures exist and match the package; Evidence: exact commit and package manifest.
- [x] 1.4 Scope: record the user's G0 decision verbatim; Out of scope: interpreting silence or starting code on conditional feedback; Expected files/components: G0 decision record and any approved planning corrections; Acceptance: only `APPROVE G0` unlocks section 2, while `REVISE G0: <feedback>` reopens tasks 1.1–1.3; IntelliJ MCP: inspect decision update; Compose MCP: not required; Evidence: user decision and exact commit.

## 2. G1 First Complete Coded Pass

- [ ] 2.1 Scope: after G0 approval, create one explicit top-level GPT-5.6 Sol High implementation packet for tasks 2.2–2.8; Out of scope: project subagents, backend integration, broad redesign, or G2/G3 polish; Expected files/components: `docs/implementation-threads/state-driven-window-sizing-g1.md`; Acceptance: packet names the approved profiles, affected files, state/ownership rules, verification commands, native-window checks, evidence boundaries, and starting commit; IntelliJ MCP: inspect packet against approved artifacts; Compose MCP: packet names exact compact/expand/shrink/focus checks; Evidence: packet path, dispatched task ID, and starting commit.
- [x] 2.2 Scope: implement the smallest pure presentation-tier and sizing-ownership policy; Out of scope: window mutation, animation, generic architecture, or changes to download state; Expected files/components: one focused Kotlin policy near `ProductWindow` and one focused test file; Acceptance: all six states map to Compact/Expanded, invalid input remains Compact, AutoManaged/UserManaged decisions are deterministic, non-floating placement suspends requests, and tests cover shrink suppression plus minimum-required growth; IntelliJ MCP: inspect callers/types and run focused tests; Compose MCP: not required; Evidence: source paths, test cases, PASS output, and exact commit.
- [x] 2.3 Scope: replace fixed launch/minimum geometry with approved compact and expanded profiles; Out of scope: motion, manual ownership, overflow, or visual restyling; Expected files/components: `Main.kt` window state/minimum handling and the minimum `ProductSurface.kt` layout changes needed to remove reserved compact height; Acceptance: normal launch shows only title bar, link row, and helper/validation status at approved compact bounds; Resolving stays compact; forced Ready reaches approved expanded bounds; no blank work-plane area appears in Compact; IntelliJ MCP: inspect/build and confirm no unresolved problems; Compose MCP: capture Empty, invalid Empty, Resolving, and Ready in light/dark at approved bounds; Evidence: bounds table, screenshots, UI-error/log checks, and exact commit.
- [x] 2.4 Scope: add one interruptible app-driven height animation and synchronize the existing body reveal; Out of scope: width animation, spring/bounce motion, custom timer framework, or new animation dependency; Expected files/components: `ProductWindow` sizing coordinator and existing `ProductBody` transition constants; Acceptance: Compact→Expanded uses 250 ms, Expanded→Compact uses 167 ms, URL field remains anchored, later-state transitions do not resize, rapid retargeting starts from current rendered height, zero-duration motion snaps, and interaction remains available; IntelliJ MCP: inspect lifecycle/cancellation/state writes and build; Compose MCP: observe normal expansion, shrink, zero-duration, and mid-flight retargeting with no visible stepping/overshoot; Evidence: timing record, bounds samples, interaction result, logs, and exact commit.
- [x] 2.5 Scope: implement user-managed sizing and platform-placement precedence; Out of scope: persisted preferences, settings UI, or custom snap behavior; Expected files/components: window sizing coordinator plus existing `WindowState`/AWT listeners; Acceptance: non-app floating resize switches to UserManaged, later states never auto-shrink it, too-small user bounds grow only to minimum usable size, manual input cancels active motion, and maximized/snapped/full-screen placement is never replaced by floating requests; IntelliJ MCP: inspect event/tolerance logic, callers, and tests; Compose MCP: exercise manual resize, state changes, maximize/restore, and resize-during-animation where supported; Native Windows proof: mandatory outer-window interaction record; Evidence: ownership trace, before/after bounds, screenshots, and exact commit.
- [x] 2.6 Scope: keep automatic targets within the active monitor work area and synchronize tier minimums; Out of scope: multi-monitor preference persistence or a general screen-management service; Expected files/components: `ProductWindow` AWT work-area calculation, tier minimum ordering, and narrow constrained-height overflow in `ProductSurface`; Acceptance: normal growth preserves top/left, near-edge growth applies only the minimum corrective shift, impossible preferred bounds cap safely, compact minimum permits the approved compact target, expanded minimum is raised only after growth, collapse lowers it before motion, and essential controls remain reachable; IntelliJ MCP: inspect density/rounding/inset calculations and build; Compose MCP: verify constrained-height overflow, minimum/preferred sizes, and no normal-condition scrollbar; Native Windows proof: near-bottom/right placement and full-window bounds; Evidence: work-area calculations, captures, interactions, and exact commit.
- [x] 2.7 Scope: extend the design-review harness only for deterministic motion and ownership reset; Out of scope: production controls, simulated native maximize/snap, or a second product layout; Expected files/components: `DesignReviewApp.kt` and the smallest optional `ProductWindow` review inputs; Acceptance: controller still forces all six states/themes/fixtures, can force normal versus zero-duration motion, can restore AutoManaged sizing, and Reset returns canonical compact Empty; IntelliJ MCP: inspect/build/run the review configuration; Compose MCP: activate every new control and verify product/controller windows remain separate; Evidence: control matrix, window IDs, screenshots, and exact commit.
- [x] 2.8 Scope: extend automated regression coverage for the complete G1 path; Out of scope: broad UI-test framework changes, per-frame screenshot tests, or slow tests in default `test`; Expected files/components: focused policy tests and minimal additions to `ProductSmokeTest.kt`; Acceptance: tests cover tier mapping, ownership decisions, stale target cancellation, happy Compact→Expanded→Completed, failure/retry staying Expanded, and Download Another returning Compact; `gradlew.bat check` and `gradlew.bat smokeTest` pass; IntelliJ MCP: run focused/full checks and report inspections; Compose MCP: not required for pure/in-process assertions; Evidence: commands, PASS results, duration, and exact commit.
- [ ] 2.9 Scope: run one separate read-only top-level GPT-5.6 Sol High G1 review after tasks 2.2–2.8; Out of scope: reviewer edits, duplicate full-suite reruns, project subagents, or G2 scope; Expected files/components: severity-ranked technical/UX review under `docs/design/reviews/state-driven-window-sizing/G1-first-pass/IMPLEMENTATION_REVIEW.md`; Acceptance: review checks root ownership, geometry loops, DPI rounding, animation interruption, user-control precedence, accessibility, test gaps, and visual coherence using existing exact-commit evidence plus only missing/finding-specific checks; IntelliJ MCP: targeted inspection/build/tests as evidence requires; Compose MCP: targeted state/window checks as evidence requires; Evidence: reviewed commit, findings, and verdict.
- [ ] 2.10 Scope: apply one coherent correction pass only if task 2.9 has accepted findings; Out of scope: repeated correction loops, unrelated refactors, or new design direction; Expected files/components: only owning files from tasks 2.2–2.8 and review note updates; Acceptance: each accepted critical/major finding is fixed or explicitly rejected with rationale, targeted verification passes, and no final G1 evidence predates the accepted commit; IntelliJ MCP: inspect/build/test changed scope; Compose MCP: verify affected transitions/states only; Evidence: correction commit or `NOT REQUIRED`, finding disposition, and targeted results.
- [ ] 2.11 Scope: capture final G1 evidence once after review/correction and package the gate; Out of scope: stale pre-review captures, G2 edge matrix, or backend work; Expected files/components: `docs/design/evidence/state-driven-window-sizing/G1/` and G1 `REVIEW.md`, `EVIDENCE.md`, `manifest.json`; Acceptance: exact accepted commit proves compact launch, Resolving stability, first expansion, later-state stability, shrink, focus, light/dark, normal/zero motion, manual resize, maximize/restore, UI errors, and logs; package ends `AWAITING USER: APPROVE G1 or REVISE G1: <feedback>`; IntelliJ MCP: confirm accepted commit builds/checks; Compose MCP: one bounded final client-area pass; Native Windows proof: same-commit full-window captures and controls; Evidence: canonical package only.
- [ ] 2.12 Scope: record the user's G1 decision verbatim; Out of scope: proceeding on conditional feedback; Expected files/components: G1 decision record and approved corrections; Acceptance: only `APPROVE G1` unlocks section 3, while `REVISE G1: <feedback>` reopens the owning G1 tasks; IntelliJ MCP: inspect decision update; Compose MCP: not required unless feedback changes visuals; Evidence: user decision and exact commit.

## 3. G2 Full-State And User-Control Refinement

- [ ] 3.1 Scope: after G1 approval, create one explicit top-level GPT-5.6 Sol High G2 packet for refinement and full matrix proof; Out of scope: project subagents, backend work, or unrelated visual redesign; Expected files/components: `docs/implementation-threads/state-driven-window-sizing-g2.md`; Acceptance: packet starts from approved G1, assigns tasks 3.2–3.5, and names all six states, normal/failure/edit/reset flows, ownership/placement, themes, scales, screen-edge, reduced-motion, and long-content checks; IntelliJ MCP: inspect packet; Compose MCP: packet names exact evidence sequence; Evidence: packet path, task ID, and starting commit.
- [ ] 3.2 Scope: refine approved profile metrics and motion only where G1 evidence shows a concrete issue; Out of scope: adding tiers, width animation, new components, or aesthetic churn; Expected files/components: existing sizing constants/coordinator and `ProductSurface` spacing only; Acceptance: compact height contains no waste, expanded states fit without normal scrolling, body/outer timing reads as one transition, URL anchor remains stable, and no later-state jitter exists; IntelliJ MCP: inspect/build; Compose MCP: compare before/after Empty→Resolving→Ready and Ready→Downloading→Completed/Error in light/dark; Evidence: measured rationale, affected captures, and exact commit or `NOT REQUIRED`.
- [ ] 3.3 Scope: harden manual resize, placement, work-area, and scaling behavior across the full state matrix; Out of scope: saved window preferences or cross-platform guarantees; Expected files/components: existing window coordinator/policy and constrained overflow only; Acceptance: AutoManaged and UserManaged paths pass at 100%/125%/150%, near each work-area edge, through maximize/restore and available snap behavior, during motion interruption, and with target changes; no control becomes unreachable or window leaves the work area; IntelliJ MCP: inspect rounding/event ordering/build/tests; Compose MCP: client layout/semantics for constrained states; Native Windows proof: placement/resize matrix; Evidence: matrix, bounds traces, captures, and exact commit.
- [ ] 3.4 Scope: harden focus, keyboard traversal, semantics, and reduced-motion behavior for window transitions; Out of scope: formal certification, screen-reader-specific hacks, or new shortcuts; Expected files/components: existing focus/semantics code and review-only motion override; Acceptance: expansion does not steal focus, revealed controls follow reading order, Download Another/Reset focus the link field, live regions do not duplicate every resize frame, progress/error meaning remains textual, and zero-duration mode has identical final bounds/content; IntelliJ MCP: inspect semantics/focus/build/tests; Compose MCP: keyboard and semantic-tree checks for Empty, Ready, Downloading, Completed, Error, and reset; Evidence: focus checklist, semantic files, reduced-motion comparison, and exact commit.
- [ ] 3.5 Scope: complete focused G2 tests and portable verification; Out of scope: new test tooling or duplicate visual assertions; Expected files/components: existing sizing-policy and smoke tests only; Acceptance: interruption, ownership tolerance, minimum ordering, work-area clamp, and all normal state-tier transitions have deterministic coverage; `gradlew.bat check` and `gradlew.bat smokeTest` pass; IntelliJ reports no unresolved problems and both run configurations launch; IntelliJ MCP: mandatory inspect/build/test/run; Compose MCP: representative Empty/Ready parity and no-error/log spot check only; Evidence: exact commit, commands, results, and known limitations.
- [ ] 3.6 Scope: run one separate read-only top-level GPT-5.6 Sol High G2 review, then at most one accepted correction pass; Out of scope: reviewer edits, repeated review loops, broad redesign, or duplicate full verification; Expected files/components: G2 `IMPLEMENTATION_REVIEW.md` and correction commit only when required; Acceptance: review covers every spec scenario, platform behavior, UX polish, accessibility, and evidence freshness; accepted findings are fixed or dispositioned before capture; IntelliJ MCP: targeted checks only; Compose MCP: targeted affected-state checks only; Evidence: reviewed commit, verdict, correction reference or `NOT REQUIRED`.
- [ ] 3.7 Scope: capture final G2 evidence once and package the refined gate; Out of scope: pre-review captures or G3 final archive work; Expected files/components: `docs/design/evidence/state-driven-window-sizing/G2/` plus G2 `REVIEW.md`, `EVIDENCE.md`, `manifest.json`; Acceptance: complete state/flow/theme/scale/ownership/placement/motion matrix describes one accepted commit, no UI error exists, logs are clean, and package ends `AWAITING USER: APPROVE G2 or REVISE G2: <feedback>`; IntelliJ MCP: confirm build/check at accepted commit; Compose MCP: one complete client-area pass; Native Windows proof: one complete outer-window pass; Evidence: canonical package.
- [ ] 3.8 Scope: record the user's G2 decision verbatim; Out of scope: proceeding on conditional feedback; Expected files/components: G2 decision record and approved corrections; Acceptance: only `APPROVE G2` unlocks section 4, while `REVISE G2: <feedback>` reopens owning G2 tasks; IntelliJ MCP: inspect decision update; Compose MCP: not required unless feedback changes visuals; Evidence: user decision and exact commit.

## 4. G3 Final Hardening And Acceptance

- [ ] 4.1 Scope: harden final edge cases without changing the approved two-tier direction; Out of scope: new features, extra tiers, width animation, backend integration, or decoration; Expected files/components: only existing window coordinator, `ProductSurface`, focus/semantics, and tests; Acceptance: long/extreme title, long destination, missing preview, disabled action, invalid URL, failure/retry, smallest approved bounds, constrained overflow, light/dark, 100%/125%/150%, keyboard-only flow, and rapid state changes remain usable and stable; IntelliJ MCP: inspect/build/tests; Compose MCP: affected edge-state matrix; Native Windows proof: only affected outer-bound cases; Evidence: hardening checklist, captures, and exact commit.
- [ ] 4.2 Scope: run final automated and IDE verification at one clean candidate commit; Out of scope: evidence capture before review or repeated green reruns without risk; Expected files/components: unchanged verification configuration unless a concrete gap requires a minimal test; Acceptance: `gradlew.bat check` and `gradlew.bat smokeTest` pass, IntelliJ inspections/build pass, Downlet and Design Review launch, and repository state is clean; IntelliJ MCP: mandatory full inspection/build/test/run; Compose MCP: no-error/log and representative parity spot check; Evidence: candidate commit and compact command/result summary.
- [ ] 4.3 Scope: run one separate read-only top-level GPT-5.6 Sol High final review; Out of scope: reviewer edits, project subagents, repeated aesthetic churn, or duplicate broad verification; Expected files/components: G3 `IMPLEMENTATION_REVIEW.md` with severity-ranked technical, UX, accessibility, and standards verdict; Acceptance: reviewer checks specs, exact candidate evidence, window ownership, platform placement, motion, focus, semantics, overflow, code simplicity, and test adequacy; IntelliJ MCP: targeted checks as findings require; Compose MCP: targeted affected-state checks as findings require; Evidence: reviewed commit and verdict.
- [ ] 4.4 Scope: apply at most one final correction pass for accepted G3 findings; Out of scope: new direction or repeated loops; Expected files/components: only files owning accepted defects; Acceptance: critical/major findings are resolved or explicitly accepted, targeted checks pass, and the final evidence commit is newer than all corrections; IntelliJ MCP: inspect/build/test changed scope; Compose MCP: affected visual checks only; Evidence: correction commit or `NOT REQUIRED` and finding disposition.
- [ ] 4.5 Scope: capture the final code-backed evidence set once and package G3 acceptance; Out of scope: mockups, stale captures, or backend claims; Expected files/components: `docs/design/evidence/state-driven-window-sizing/G3/` plus G3 `REVIEW.md`, `EVIDENCE.md`, `manifest.json`; Acceptance: exact accepted commit proves every spec scenario, four-gate history, themes, scales, states, flows, focus, semantics, reduced motion, manual ownership, maximize/restore, screen-edge handling, title bar, window controls, UI errors, and logs; package ends `AWAITING USER: APPROVE G3 or REVISE G3: <feedback>`; IntelliJ MCP: confirm final build/check; Compose MCP: one final complete client-area pass; Native Windows proof: one final complete outer-window pass; Evidence: canonical final package.
- [ ] 4.6 Scope: record `APPROVE G3` verbatim and reconcile task/spec status; Out of scope: inferring approval or starting backend work; Expected files/components: G3 decision record and accurate checkbox/spec status; Acceptance: every completed task has evidence, no unimplemented behavior is marked complete, and approval names the final accepted commit; IntelliJ MCP: final status/commit spot check; Compose MCP: unchanged final no-error spot check only if evidence became stale; Evidence: approval commit.

## 5. Closeout

- [ ] 5.1 Scope: synchronize and archive this change through the current OpenSpec workflow after G3 approval; Out of scope: starting yt-dlp integration or deleting historical evidence; Expected files/components: main `state-driven-window-sizing` spec, reconciled active design change, and archived change artifacts; Acceptance: strict validation passes, main specs match accepted behavior, no fixed-window contradiction remains in live requirements, and archive references the final approved commit; IntelliJ MCP: inspect final planning/spec state; Compose MCP: not required after unchanged accepted proof; Evidence: validation and archive results plus final commit.
