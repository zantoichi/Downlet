## 1. G0 Direction And Gate

- [x] 1.1 Scope: reconcile the canonical prompt, product assumptions, Impeccable context, and empty-repository state; Out of scope: Kotlin code; Expected: `PRODUCT.md`, `DESIGN.md`, canonical prompt check, IntelliJ project inventory; Acceptance: Windows-first YouTube user and basic accessibility baseline are recorded and the canonical `D:` prompt matches the attachment; IntelliJ MCP: confirm module/dependency/run-configuration state; Compose MCP: not applicable before scaffold; Evidence: G0 review notes.
- [x] 1.2 Scope: verify current standalone-compatible Jewel and Compose Hot Reload APIs; Out of scope: dependency installation; Expected: source-backed component/version notes; Acceptance: every proposed significant element has a named current API or justified Compose primitive and no low-confidence effect remains; IntelliJ MCP: confirm current project has no conflicting dependency context; Compose MCP: not applicable; Evidence: `docs/design/reviews/G0-direction/CODABILITY.md` sources and risks.
- [x] 1.3 Scope: create the OpenSpec proposal, capability specs, and technical design; Out of scope: application implementation; Expected: `proposal.md`, three capability specs, `design.md`; Acceptance: strict validation passes and all six states, fake behavior, review harness, accessibility baseline, gates, and backend exclusions are specified; IntelliJ MCP: inspect files for parse or encoding problems; Compose MCP: not applicable; Evidence: OpenSpec validation output.
- [x] 1.4 Scope: produce the G0 review package and send it to a separate top-level GPT-5.6 Sol High review task; Out of scope: subagents, generated evidence, or app screenshots; Expected: `docs/design/reviews/G0-direction/REVIEW.md`, `CODABILITY.md`, prompt-source note, and reviewer findings; Acceptance: one recommended direction, alternatives rejected, manual review checklist, compromises, approval syntax, and accepted critique fixes are recorded; IntelliJ MCP: inspect final Markdown files; Compose MCP: not applicable; Evidence: committed G0 artifacts and top-level review result.
- [x] 1.5 Scope: record the user's gate decision verbatim; Out of scope: inferring approval; Expected: G0 `REVIEW.md` decision field; Acceptance: only `APPROVE G0` unlocks G1 and `REVISE G0: <feedback>` reopens direction work; IntelliJ MCP: not applicable; Compose MCP: not applicable; Evidence: review artifact update and approval commit.

## 2. G1 Scaffold, Tooling, And State Foundation

- [x] 2.1 Scope: create the first narrow top-level Sol High implementation dispatch packet for scaffold/smoke tasks; Out of scope: implementing in the root task or any subagent; Expected: `docs/implementation-threads/01-g1-scaffold.md`; Acceptance: packet names base commit, tasks 2.2-2.3, exact context, model GPT-5.6 Sol, reasoning High, IntelliJ checks, and return contract; IntelliJ MCP: verify packet file; Compose MCP: not applicable before MCP configuration; Evidence: packet path and dispatched task ID.
- [x] 2.2 Scope: create a minimal single-module Kotlin/JVM Compose Desktop Gradle scaffold and wrapper; Out of scope: product UI beyond a compile-only entry point, packaging, extra libraries, multi-module structure; Expected: `settings.gradle.kts`, `build.gradle.kts`, `gradle.properties`, Gradle wrapper, `src/main/kotlin/downlet/Main.kt`; Acceptance: pinned Kotlin, Compose, Jewel standalone, Hot Reload, repositories, Kotlin `JvmTarget.JVM_21`, Java `--release 21`, JBR 25 runtime provisioning, and main class resolve with no unused architecture; IntelliJ MCP: refresh project and inspect module/dependencies; Compose MCP: not yet required; Evidence: changed files and dependency-resolution result.
- [x] 2.3 Scope: prove the pinned stack with the smallest `IntUiTheme` plus `DecoratedWindow` smoke window; Out of scope: final layout, product controls, review controller, or Hot Reload MCP proof; Expected: temporary/minimal app composition; Acceptance: Jewel `0.39.1-262.9437.29`, Compose `1.11.0`, Kotlin `2.3.20`, Hot Reload `1.2.0`, JVM target 21, JBR 25 runtime, decorated-window styling, and the plain title bar compile and launch together; IntelliJ MCP: inspect changed Kotlin, run `build_project`, and launch the main entry; Compose MCP: not required until task 2.9; Evidence: build and launch summary.
- [x] 2.4 Scope: create the second narrow top-level Sol High implementation dispatch packet for product foundation and review tooling; Out of scope: implementing in the root task or any subagent; Expected: `docs/implementation-threads/02-g1-foundation.md`; Acceptance: packet names the successful 2.2-2.3 base commit and assigns tasks 2.5-2.9 in dependency order with exact MCP/test/return requirements; IntelliJ MCP: verify packet file; Compose MCP: not applicable to the packet itself; Evidence: packet path and dispatched task ID.
- [x] 2.5 Scope: implement the normal product window shell and theme source; Out of scope: navigation, project stripe, menu, toolbar, IDE actions, or content states; Expected: product `DecoratedWindow`, plain `TitleBar`, `WindowState`, decorated `IntUiTheme`, `isSystemInDarkTheme()` startup source, root surface; Acceptance: initial size is near 720×420, title is Downlet, only standard Windows window controls appear, normal launch adopts the current Windows theme with light fallback, restart picks up later OS changes, and explicit review overrides do not recreate product state; IntelliJ MCP: inspect/reformat changed Kotlin and run `build_project`; Compose MCP: capture client-area light/dark shell screenshots and window bounds; Native Windows proof: use Codex Computer Use `Windows.Graphics.Capture` for light and dark full-window screenshots and record controls, drag, and maximize/restore at the same commit; Evidence: source paths, Compose captures, native captures, and interaction note.
- [x] 2.6 Scope: implement immutable product state, events, normal fixtures, and tiny state holder; Out of scope: backend interfaces, dependency injection, real I/O, full G2 transition engine; Expected: state/event/fixture files under `src/main/kotlin/downlet`; Acceptance: all six states are representable, invalid combinations are avoided, and normal/long/missing/error fixtures are deterministic; IntelliJ MCP: inspect symbols, callers, and file problems; Compose MCP: not required for pure state; Evidence: source paths and focused state tests.
- [x] 2.7 Scope: implement the separate developer-only Design Review Controller shell; Out of scope: shipping controller UI or every G2 control; Expected: review entry point and controller composable; Acceptance: review launch opens product and controller windows, controller can switch Empty/Ready and explicit light/dark regardless of Windows preference, and normal launch contains no controller; IntelliJ MCP: inspect/build; Compose MCP: not required until task 2.9; Evidence: source paths and build result.
- [x] 2.8 Scope: add shared IntelliJ run configurations for the normal app and `Design Review`; Out of scope: packaging or OS shortcuts; Expected: `.run/Downlet.run.xml`, `.run/Design Review.run.xml` or equivalent shared configurations; Acceptance: normal entry opens one product window and review entry opens the product plus controller entry points created in 2.7; IntelliJ MCP: list and execute both configurations; Compose MCP: not required until task 2.9; Evidence: exact configuration names and IntelliJ launch results.
- [x] 2.9 Scope: configure project-local Compose Hot Reload MCP startup through `hotMcpServer`; Out of scope: custom MCP server or title-bar screenshot claims; Expected: minimal Codex/MCP project configuration and Gradle task availability; Acceptance: MCP connects to the running `Design Review` app and exposes status, reload, client-area screenshots, windows, semantics, interactions, resize, errors, and logs; IntelliJ MCP: verify Gradle task and build; Compose MCP: call `status` and `list_windows` and distinguish client-area captures from native full-window evidence; Evidence: configuration path and connection transcript summary.

## 3. G1 Core Surface And Interaction

Current order after G1 approval: tasks 3.30–3.34 are the immediate priority. Do not start task 4.1 until task 3.34 passes at one clean exact commit.

- [x] 3.1 Scope: create the third narrow Sol High dispatch packet for G1 URL/Empty/Resolving tasks; Out of scope: media/options and G2 states; Expected: `docs/implementation-threads/03-g1-url-flow.md`; Acceptance: packet assigns tasks 3.2-3.6 from the approved base commit with exact return contract; IntelliJ MCP: inspect packet; Compose MCP: packet requires only relevant URL-flow checks; Evidence: packet path and dispatched task ID.
- [x] 3.2 Scope: implement the persistent YouTube-link row with visible `YouTube link` label, state-based Jewel `TextField`, `Paste a YouTube link…` placeholder, and outlined Paste button; Out of scope: clipboard read, validation, or resolving transition; Expected: link-row composable; Acceptance: row aligns to the form grid, fills width at default/minimum sizes, and remains visible in Empty and Ready; IntelliJ MCP: inspect/reformat/build; Compose MCP: screenshots at 720×420 and 620×350 plus semantic names; Evidence: changed files and screenshots.
- [x] 3.3 Scope: implement the stdlib-only YouTube URL predicate and inline validation model; Out of scope: network validation, provider-general parsing, redirects; Expected: validation function and tests; Acceptance: agreed YouTube hosts pass, malformed/unsupported values fail, invalid text remains editable, and `Enter a valid YouTube link.` appears; IntelliJ MCP: inspect callers and run tests/build; Compose MCP: type invalid text and confirm inline validation without state transition; Evidence: test result and semantic-tree excerpt.
- [x] 3.4 Scope: implement the Paste affordance through a tiny Windows AWT clipboard adapter; Out of scope: clipboard history, permissions UI, non-text formats; Expected: edge callback and Paste event handling; Acceptance: valid clipboard URL populates the field and enters Resolving immediately, invalid clipboard text remains editable with validation, clipboard failures are non-crashing; IntelliJ MCP: inspect error handling/build/tests; Compose MCP: activate Paste with valid and invalid prepared clipboard content; Evidence: interaction results and logs.
- [x] 3.5 Scope: implement Ctrl+V immediate intent and 350 ms manual-typing debounce; Out of scope: global shortcuts or custom text editor; Expected: key-event and text-observation logic; Acceptance: keyboard paste resolves immediately after the edit, manual typing resolves only after idle, Enter is unnecessary, and paste intent clears after one edit; IntelliJ MCP: inspect state flow/coroutines and run focused tests/build; Compose MCP: type and paste through the field, observing state timing; Evidence: tests and interaction transcript.
- [x] 3.6 Scope: implement Empty and fake Resolving bodies with fixed copy; Out of scope: Ready media/options; Expected: Empty copy and resolving activity composable; Acceptance: Empty shows `Paste a YouTube link to choose video or audio.` and no premature controls; Resolving preserves the link and uses Jewel `CircularProgressIndicator` with `Checking this YouTube link…` for a fixed 550 ms transition; IntelliJ MCP: inspect/build; Compose MCP: capture Empty light/dark and Resolving, inspect semantics and UI errors; Evidence: screenshots, semantic captures, logs.
- [x] 3.7 Scope: create the fourth narrow Sol High dispatch packet for the G1 Ready composition and focused tests; Out of scope: verification packaging; Expected: `docs/implementation-threads/04-g1-ready-surface.md`; Acceptance: packet assigns tasks 3.8-3.14 with approved visual constraints and no redesign authority; IntelliJ MCP: inspect packet; Compose MCP: packet lists exact Ready checks; Evidence: packet path and dispatched task ID.
- [x] 3.8 Scope: implement the Ready media identity row and bundled deterministic thumbnail; Out of scope: remote image loading, views/date/codec metadata; Expected: media-header composable and local resource; Acceptance: 16:9 geometry, two-line title, channel/duration/YouTube metadata, and stable missing-image seam fit default/minimum sizes; IntelliJ MCP: inspect resources/Kotlin/build; Compose MCP: capture normal Ready at default and minimum sizes; Evidence: screenshots and semantic tree.
- [x] 3.9 Scope: implement mutually exclusive Video/Audio choice with Jewel `RadioButtonRow`; Out of scope: codec/container controls; Expected: format-row composable and event handling; Acceptance: Video defaults selected, whole labeled rows are clickable/keyboard accessible, Audio changes state, disabled state is available for later use; IntelliJ MCP: inspect/build/tests; Compose MCP: click and keyboard-toggle both options, inspect selected semantics; Evidence: interaction and semantic results.
- [x] 3.10 Scope: implement the quality selector with Jewel `ListComboBox`; Out of scope: format IDs, custom combo implementation, searchable list; Expected: quality-row composable and deterministic option fixtures; Acceptance: control has stable width, selected video label reads `Best available — 2160p`, selected audio label names resolved best audio quality, and popup options are concise; IntelliJ MCP: inspect named API use/build/tests; Compose MCP: open selector, choose options, inspect selected value and minimum-size popup; Evidence: screenshots and interaction result.
- [x] 3.11 Scope: implement the destination row and deterministic Change action; Out of scope: native file chooser, filesystem access, persistence; Expected: destination-row composable; Acceptance: Downloads appears initially, full value is semantic, long text truncates without moving Change, and Change cycles fixed fixtures while showing `Save location changed to {destination}.`; IntelliJ MCP: inspect/build/tests; Compose MCP: activate Change and capture normal/long values plus acknowledgement; Evidence: screenshots and semantic tree.
- [x] 3.12 Scope: implement the single Ready Download primary action and temporary G1 feedback; Out of scope: progress engine, Downloading state, or real download; Expected: Ready action row, event, and inline acknowledgement; Acceptance: Jewel `DefaultButton` is the only primary action, enabled state follows valid Ready data, and activation visibly shows `Design preview: Download action received.` without leaving Ready; G2 replaces this acknowledgement with the deterministic Downloading transition; IntelliJ MCP: inspect/build/tests; Compose MCP: verify visual hierarchy, pointer/keyboard activation, visible acknowledgement, and disabled fixture; Evidence: screenshot and interaction result.
- [x] 3.13 Scope: implement default/minimum-height metrics and overflow fallback; Out of scope: mobile breakpoints or broad responsive system; Expected: compact-height layout branch and bounded vertical overflow support; Acceptance: Ready remains coherent at 720×420 and 620×350, no essential action clips, and normal size shows no unnecessary scrollbar; IntelliJ MCP: inspect layout code/build; Compose MCP: resize to exact default/minimum/modestly larger bounds and capture each; Evidence: size table and screenshots.
- [x] 3.14 Scope: add focused G1 tests for URL validation, paste intent, debounce, format/quality changes, destination fixture changes, and state reset; Out of scope: screenshot golden framework or full UI test suite; Expected: smallest runnable unit/state tests; Acceptance: tests fail on the identified interaction regressions and pass deterministically; IntelliJ MCP: run tests and `build_project`; Compose MCP: not required for pure logic; Evidence: test names and PASS result.
- [x] 3.15 Scope: verify the integrated G1 commit in a separate read-only top-level GPT-5.6 Sol High review task; Out of scope: subagents, source edits, or visual redesign; Expected: findings only; Acceptance: changed files have no unresolved inspection errors, formatting is clean, `build_project` passes, tests pass, and both run configurations launch; any defect reopens its owning task IDs for a separate top-level Sol High implementation re-dispatch; IntelliJ MCP: mandatory inspections, build, tests, execute run configuration; Compose MCP: not part of this task; Evidence: exact commit, changed files, inspection/build/test/run summary.
- [x] 3.16 Scope: capture G1 product evidence with Compose MCP plus native Windows frame proof; Out of scope: controller screenshots or generated mockups; Expected: Empty light/dark, Ready light/dark, Ready minimum-size client-area screenshots, Empty/Ready semantic trees, and Codex Computer Use `Windows.Graphics.Capture` light/dark full-window screenshots; Acceptance: paste and typing paths work, exact sizes are tested, no UI error exists, logs have no unexplained failure, and the title bar/theme/controls/drag/maximize record matches the same commit; IntelliJ MCP: confirm reviewed commit still builds; Compose MCP: status, windows, resize, click/type, client-area screenshots, semantics, errors, logs; Native Windows proof: Computer Use full-window captures and manual interaction checklist; Evidence: files under `docs/design/evidence/G1/`.
- [x] 3.17 Scope: run a read-only Impeccable critique in a separate top-level GPT-5.6 Sol High review task, then orchestrate at most one coherent fix/reverification pass; Out of scope: reviewer/root source edits, redesigning approved G0 direction, or repeated churn; Expected: critique notes and, only when needed, a narrow implementation re-dispatch using reopened owning task IDs; Acceptance: critical/major findings are fixed by a separate top-level Sol High implementation task or documented, build/tests remain green, and final evidence replaces pre-fix captures; IntelliJ MCP: inspect/build after any re-dispatch; Compose MCP: recapture affected views and recheck errors/logs; Evidence: critique summary, implementation task reference if used, and final evidence set.
- [x] 3.18 Scope: create the G1 review package and stop; Out of scope: G2 implementation; Expected: `docs/design/reviews/G1-core-surface/REVIEW.md`, `EVIDENCE.md`, `manifest.json`; Acceptance: artifact records exact commit, OpenSpec task IDs, run configuration, proof results, manual steps, strengths, compromises, 3-7 inspection points, and approval syntax; IntelliJ MCP: verify artifact files and commit; Compose MCP: evidence links resolve to final captures; Evidence: committed review package ending `AWAITING USER: APPROVE G1 or REVISE G1: <feedback>`.
- [x] 3.19 Scope: reconcile the user's G1 feedback into the approved design/OpenSpec artifacts and create one narrow revision packet; Out of scope: application-source edits or G2 work; Expected: revised proposal/design/specs, superseded G1 package status, and `docs/implementation-threads/07-g1-modern-polish-revision.md`; Acceptance: the visible Paste action is removed from the current contract, automatic native paste/typing behavior and exact motion/tonal constraints are decision-complete, `APPROVE G1 REVISION` is recorded, and the packet assigns only task 3.20; IntelliJ MCP: inspect changed artifacts; Compose MCP: packet names exact implementation checks; Evidence: planning commit and dispatched task ID.
- [x] 3.20 Scope: implement the approved G1 modern-polish revision in one top-level GPT-5.6 Sol High task on shared `main`; Out of scope: subagents, worktrees, backend behavior, new dependencies, generic animation/design systems, or G2/G3 states; Expected: remove the visible Paste/AWT clipboard path, preserve Ctrl+V immediate resolution plus 350 ms typed debounce, add initial/reset focus, one cool tonal work plane, rounded preview treatment, concise disabled reason, stronger missing-preview fallback, and one 180–220 ms fade/rise transition; Acceptance: Empty and Ready remain coherent at 720×420 and 620×350 in light/dark, all 17 existing tests plus focused revision tests pass, IntelliJ inspections/build/run configurations pass, Compose MCP interactions/screenshots/errors/logs pass, and only task 3.20 is checked; Evidence: exact commit, changed files, test/build/run summary, screenshots, and known issues. Reopened once by task 3.21 for the missing-preview text-contrast correction recorded in `docs/design/evidence/G1/g1-revision-review.md`.
- [x] 3.21 Scope: after task 3.28, run one separate read-only top-level GPT-5.6 Sol High review combining technical verification with Impeccable product/polish/animate critique of the final G1 revision and code-health refactor; Out of scope: reviewer edits, subagents, visual scope growth, repeated review loops, or automatic duplication of the implementation task's complete checks; Expected: one severity-ranked verdict against tasks 3.20 and 3.26–3.28; Acceptance: the reviewer reuses exact-commit implementation evidence, reruns only missing or stale checks and finding reproductions, batches all accepted findings, and triggers at most one correction implementation task by reopening owning task IDs; IntelliJ MCP: targeted inspect/build/test/run only when evidence is missing, stale, or needed to reproduce a finding; Compose MCP: targeted state/size/theme/interaction/error/log checks on the same basis; Evidence: review task ID and committed review note.
- [x] 3.22 Scope: after task 3.21 and any accepted correction, run one complete final revised G1 evidence pass from the accepted implementation commit; Out of scope: pre-review captures, stale captures, generated mockups, controller screenshots, duplicate full passes, or G2 states; Expected: canonical Empty/Ready light/dark/default/minimum/edge screenshots, semantic results, interaction matrix, and required native Windows full-frame proof; Acceptance: no visible Paste action exists, automatic paste/typing works, focus/disabled/fallback/motion claims are evidenced, UI errors are absent, logs are clean, and every file names the exact reviewed commit; IntelliJ MCP: confirm accepted commit still builds; Compose MCP: full relevant tool suite once; Computer Use: only the required `Windows.Graphics.Capture` frame proof when Compose cannot capture title chrome; Evidence: replaced canonical files under `docs/design/evidence/G1/`.
- [x] 3.23 Scope: rebuild the G1 review package for the revised implementation and stop; Out of scope: G2 implementation or inferred approval; Expected: updated `REVIEW.md`, `EVIDENCE.md`, and `manifest.json`; Acceptance: old package is clearly superseded, exact final commit/tasks/proof/manual steps/strengths/compromises/inspection points are current, and the package ends with the normal G1 approval syntax; IntelliJ MCP: verify files and commit; Compose MCP: evidence references resolve to final captures; Evidence: committed package ending `AWAITING USER: APPROVE G1 or REVISE G1: <feedback>`.

## 3A. Priority Code Health Before G1 Finalization

- [x] 3.24 Scope: make OpenSpec the sole current planning source, retire the external initial prompt after migrating any nonredundant rules, and make code health the next implementation priority; Out of scope: application-source edits or behavior changes; Expected: coherent proposal/design/tasks/config/source records; Acceptance: strict OpenSpec validation passes, tasks 3.25–3.28 precede G1 final review, the process uses lean verification ownership, and no arbitrary 100-line file limit is introduced; IntelliJ MCP: inspect changed planning files; Compose MCP: not required; Evidence: planning commit and retired-source record.
- [x] 3.25 Scope: dispatch one top-level GPT-5.6 Sol Medium implementation task for tasks 3.26–3.28 directly from the exact clean planning commit on shared `main`; Out of scope: root application edits, subagents, worktrees, packet creation, dispatch-record-only commits, or READY/RELEASE handshakes when the clean expected base matches; Expected: one direct implementation prompt with exact base commit, scope, checks, and return contract; Acceptance: the implementation task verifies the expected clean base before mutation and returns its task ID in completion evidence; IntelliJ MCP: identify the saved project and exact base; Compose MCP: required only by task 3.28; Evidence: implementation task ID and base commit.
- [x] 3.26 Scope: automate Kotlin formatting and static analysis with project-local Gradle tasks; Out of scope: CI-provider workflows, Git hooks, lint baselines, architecture frameworks, custom rule plugins, or unrelated dependencies; Expected: `.editorconfig`, a pinned compatible `org.jlleitschuh.gradle.ktlint` plugin, a pinned compatible official Detekt Gradle plugin, and the smallest `config/detekt/detekt.yml` needed for narrow Compose exceptions; Acceptance: stable releases are preferred, any Detekt prerelease is used only when no stable release supports the existing Kotlin/Gradle/JDK stack and its rationale is recorded, generated/build output is excluded, no file-wide suppression or baseline hides findings, `gradlew.bat check` runs formatting checks, Detekt, and tests, and `ktlintFormat` is available; IntelliJ MCP: sync, inspect Gradle/config files, and build; Compose MCP: not required; Evidence: compatibility source, versions, changed files, quality-task output, and zero unresolved IntelliJ problems.
- [x] 3.27 Scope: perform a behavior-preserving responsibility refactor of the current G1 Kotlin source and tests; Out of scope: visual, timing, focus, semantics, resource, run-configuration, state-contract, or interaction changes; new dependencies, layers, repositories, interfaces, dependency injection, or framework abstractions; Expected: keep `Main.kt` focused on entry/theme/window wiring, isolate product-surface/link effects/state-body responsibilities, isolate Ready media/form UI, separate immutable models/fixtures from mutable state transitions, and split tests only along matching ownership where useful; Acceptance: all existing 18 tests pass unchanged plus only minimal move-regression tests if needed, files have cohesive responsibilities without enforcing a hard 100-line cap, and `DesignReview.kt` may remain intact when splitting adds no value; IntelliJ MCP: use semantic moves/refactors, format, inspect, build, and run focused tests; Compose MCP: compare representative Empty and Ready light/minimum views for parity; Evidence: responsibility map, changed files, test/build results, and parity captures.
- [x] 3.28 Scope: verify tasks 3.26–3.27 at one clean exact commit inside the same implementation task; Out of scope: the final G1 review/package, full G1 evidence recapture, or repeated equivalent verification; Expected: integrated quality and parity evidence; Acceptance: worktree is clean, `gradlew.bat check` passes, IntelliJ reports zero unresolved problems, `build_project` and focused tests pass, both run configurations launch, and representative Empty/Ready light/minimum interactions, errors, and logs remain green; IntelliJ MCP: mandatory inspect/build/test/run; Compose MCP: bounded representative parity checks only; Evidence: exact commit, changed files, quality reports, launch/parity results, and known issues.

## 3B. Approved Technical Foundation Before G2

- [x] 3.29 Scope: record the user's `APPROVE G1` decision and reconcile the approved dependency, KStateMachine, progressive-disclosure, generated-resource, smoke-test, and warning-handling decisions into existing OpenSpec artifacts; Out of scope: application-source edits; Expected: approved G1 review records plus coherent proposal/design/specs/tasks; Acceptance: strict OpenSpec validation passes, G2 remains blocked behind tasks 3.30–3.34, and rejected 100-line/Git-hook/modulith frameworks stay rejected; IntelliJ MCP: inspect changed planning/review files; Compose MCP: not required; Evidence: planning commit.
- [x] 3.30 Scope: dispatch one top-level GPT-5.6 Sol Medium implementation task for tasks 3.31–3.34 directly from the exact clean planning commit on shared `main`; Out of scope: root application edits, subagents, worktrees, packet creation, READY/RELEASE handshakes, G2 visual changes, or reviewer work; Expected: one direct implementation prompt with exact base commit, scope, checks, and return contract; Acceptance: implementor verifies the expected clean base before mutation, uses IntelliJ MCP, commits one coherent result, and returns its task ID and exact commit; IntelliJ MCP: identify saved project and exact base; Compose MCP: bounded parity only in task 3.34; Evidence: task `01a04b2e-dd71-74b1-9666-06bce0a3c0b9`, base `b13c5b9`, implementation commit `e5cbcb5cce112ed4e212bb3c3d301df741da0b9a`.
- [x] 3.31 Scope: move the build to the latest stable mutually compatible dependency set and remove actionable runtime/build warnings; Out of scope: prerelease version chasing, replacing or vendoring Jewel, packaging, backend work, or suppressing upstream warnings; Expected: Kotlin `2.4.10`, target Compose `1.12.0` with evidence-backed fallback only to newest compatible stable `1.11.x`, Gradle `9.5.0`, current stable Jewel/Hot Reload/ktlint/Detekt/coroutines/Foojay pins, and JBR native-access launch arguments; Acceptance: dependencies resolve, app/tests still target JVM 21 on JBR 25, Skiko's restricted `System.load` warning is removed with `--enable-native-access=ALL-UNNAMED`, Jewel's current `Unsafe` warning is documented as upstream-only rather than hidden, and configuration cache is enabled only after two clean reuse runs for `check`, `smokeTest`, `hotRun`, and `hotMcpServer` without persistent warning mode; IntelliJ MCP: sync, inspect build files/dependencies, build, and launch; Compose MCP: confirm Hot Reload still connects; Evidence: compatibility sources, final versions, warning before/after, cache reuse output, and launch result.
- [x] 3.32 Scope: integrate `io.github.nsk90:kstatemachine-coroutines:0.38.1` as the normal Empty/Resolving/Ready transition engine while preserving approved G1 behavior; Out of scope: G2 Downloading/Completed/Error behavior, hierarchy, parallel regions, serialization, undo, generated diagrams, a wrapper framework, or UI redesign; Expected: one flat state machine owned by `DownloadStateHolder`, existing immutable render state/data, explicit UI/internal events, timer completion events, deterministic controller forcing, and lifecycle cleanup; Acceptance: paste remains immediate, typing remains 350 ms debounced, resolution remains 550 ms and stale work remains cancelled, forced states bypass timers, all existing tests pass, and focused transition/cleanup tests prove the machine is the normal-flow source of truth; IntelliJ MCP: inspect dependency APIs, call flow, lifecycle, problems, tests, and build; Compose MCP: no visual claim beyond task 3.34 parity; Evidence: source paths, state/transition map, tests, and dependency version.
- [x] 3.33 Scope: add the smallest fast in-process product smoke suite; Out of scope: Selenium/Appium, screenshots/goldens, broad UI-test infrastructure, native window automation, a second test framework, or network/process/filesystem behavior; Expected: Compose desktop UI-test dependency, semantics-driven product test, and a `smokeTest` Gradle command; Acceptance: `gradlew.bat smokeTest` renders the real `ProductSurface`, drives Empty→Resolving→Ready with virtual time, checks key accessible controls/state, reports wall time, targets a warm run under ten seconds on the reference Windows host without making timing a portable correctness failure, and remains separate from `check`; IntelliJ MCP: inspect/run the smoke test and build; Compose MCP: not required for in-process test; Evidence: command, assertions covered, duration, and result.
- [x] 3.34 Scope: verify tasks 3.31–3.33 at one clean exact commit inside the same implementation task; Out of scope: G2 UI, generated concepts/resources, broad evidence recapture, or independent review; Expected: integrated technical-foundation proof; Acceptance: worktree is clean, `gradlew.bat check` and `gradlew.bat smokeTest` pass, configuration-cache reuse is proven or left disabled with exact blocker, IntelliJ reports zero unresolved problems and `build_project` passes, Downlet and Design Review launch, Hot Reload connects, and representative Empty/Ready visuals/interactions remain unchanged; IntelliJ MCP: mandatory inspect/build/test/run; Compose MCP: bounded Empty/Ready parity, errors, and logs only; Evidence: exact commit, changed files, test/build/cache/launch/parity results, and known upstream warning.

## 4. G2 Full Fake State System

- [x] 4.1 Scope: after task 3.34, create one narrow Sol High G2 dispatch packet for generated-resource direction, progressive disclosure, complete deterministic state work, and focused tests; Out of scope: G3 hardening; Expected: `docs/implementation-threads/05-g2-state-system.md`; Acceptance: packet assigns tasks 4.2–4.9 from the clean technical-foundation commit, preserves the approved Quiet Signal Reveal direction, and allows no unrelated redesign; IntelliJ MCP: inspect packet; Compose MCP: packet names exact state/interaction/evidence checks; Evidence: packet commit `c283fe939bdcc0f2bef905770ca56e80f634992d`, task `01a04b5e-6b24-7e22-be38-a9e773002575`, implementation commit `97e25d586c61ae0dfdae1896048d3b2a72a30dc3`.
- [x] 4.2 Scope: extend the KStateMachine normal flow with the fixed fake download timeline, failure, cancellation, retry, completion, and reset transitions; Out of scope: real transfer, speed calculation, a second state engine, or coroutine abstraction layer; Expected: flat machine transitions plus smallest timer effects; Acceptance: progress follows 0/18/43/68/87/100 every 350 ms, success completes, failure fixture errors at 68, Cancel returns Ready, Retry restarts, reset returns Empty, stale jobs cannot transition, and forced states bypass timers; IntelliJ MCP: inspect machine transitions/coroutine cancellation/callers/build/tests; Compose MCP: observe happy/failure/cancel/retry paths; Evidence: transition map, tests, and interaction timing note. Reopened by task 4.10 for stale same-fixture progress across forced-state boundaries.
- [x] 4.3 Scope: implement Quiet Signal Reveal progressive disclosure and the Downloading visual block; Out of scope: telemetry dashboard, extra controls, animation framework, or decorative background; Expected: Empty link/helper only, compact Resolving status, one revealed tonal work plane for Ready and later states, determinate `HorizontalProgressBar`, percentage, speed/time copy, and locked choices; Acceptance: URL field/window stay fixed, Empty draws no work plane, Resolving reveals no premature controls, Ready reveals the plane once in 180–220 ms, media context stays visible during Downloading, progress semantics update, and format/quality/destination cannot change; IntelliJ MCP: inspect/build; Compose MCP: capture Empty/Resolving/Ready/Downloading in light/dark and inspect motion/progress/disabled semantics; Evidence: screenshots, interaction timing, and semantic capture. Reopened by task 4.10 because later-state transitions recreate and reanimate the shared work plane.
- [x] 4.3a Scope: generate and distill the Quiet Signal Reveal visual resources before final G2 implementation capture; Out of scope: AI-rendered text, YouTube branding, raw generated full-window art in production, glass/noise backgrounds, or multiple illustration families; Expected: three non-shipping Empty/Resolving/Ready concept references plus focused app-icon/thumbnail/fallback studies, followed by at most one app icon family, one deterministic 16:9 thumbnail, and one themeable missing-preview mark in Compose resources; Acceptance: selected resources match light/dark Jewel surfaces, remain legible at product sizes, use vector/SVG where practical, and introduce no new runtime image dependency; IntelliJ MCP: inspect resources/build; Compose MCP: compare final resource use in Ready/missing-preview light/dark; Evidence: concept paths, selection note, production resource paths, and screenshots. Reopened by task 4.10 for fallback copy and frame-and-signal resource correction.
- [x] 4.4 Scope: implement Cancel as a calm secondary Jewel link; Out of scope: confirmation dialog or backend cancellation protocol; Expected: Downloading action; Acceptance: Cancel stops the fake job and returns to Ready with mode, quality, destination, and media preserved; IntelliJ MCP: inspect job cancellation/build/tests; Compose MCP: click and keyboard-activate Cancel; Evidence: tests and before/after state capture.
- [x] 4.5 Scope: implement Completed status and primary/secondary actions; Out of scope: real file/folder access or celebration animation; Expected: Completed composable; Acceptance: `Saved to {destination}` is visible, Open Folder is the only primary button, Download Another is secondary, and status is not color-only; IntelliJ MCP: inspect/build; Compose MCP: capture Completed light/dark and semantics; Evidence: screenshots and semantic capture.
- [x] 4.6 Scope: implement fake Open Folder and Download Another/reset behavior; Out of scope: launching Explorer or filesystem checks; Expected: completion event handling; Acceptance: Open Folder keeps Completed visible and shows `Folder opening is unavailable in this design preview.`; Download Another resets to Empty and restores link-field focus; IntelliJ MCP: inspect/build/tests; Compose MCP: activate both actions and inspect acknowledgement/focus/state; Evidence: interaction and focus result.
- [x] 4.7 Scope: implement recoverable Error with non-deprecated Jewel `InlineErrorBanner` and Retry; Out of scope: stack trace/details UI, raw process text; Expected: Error composable and event handling; Acceptance: banner fills available width, `Couldn't download this media.`, `Check that the YouTube link is available and try again.`, and Retry are visible; media context remains and Retry restarts fake Downloading; IntelliJ MCP: inspect API/deprecations/build/tests; Compose MCP: capture Error light/dark, activate Retry, inspect semantics; Evidence: screenshots and interaction result. Reopened by task 4.10 to use the native `linkActions` slot.
- [x] 4.8 Scope: complete Design Review Controller controls for all six states and all required fixtures; Out of scope: production navigation or shared product layout; Expected: controller controls; Acceptance: every state/theme/fixture can be forced instantly and Reset returns the canonical Empty baseline; IntelliJ MCP: inspect/build/execute review configuration; Compose MCP: click every control and verify product window separately; Evidence: controller coverage checklist and window IDs.
- [x] 4.9 Scope: add focused G2 tests for progress timeline, failure, cancellation, retry, completion, reset, and forced-state timer bypass, then extend the existing smoke path through the full happy and recoverable-failure flows; Out of scope: broad UI-test framework or new test tooling; Expected: state tests plus the existing `smokeTest` suite; Acceptance: deterministic tests cover every transition and coroutine cancellation edge, the product smoke reaches Completed on the happy path and Error→Retry on the failure path with virtual time, and elapsed wall time remains reported; IntelliJ MCP: run tests/smoke/build; Compose MCP: not required for in-process tests; Evidence: PASS and duration summary. Reopened by task 4.10 for one production-like stale-progress regression test.
- [x] 4.10 Scope: after tasks 4.2–4.9, run one separate read-only top-level GPT-5.6 Sol High review combining technical verification with Impeccable critique; Out of scope: subagents, reviewer edits, visual redesign, separate duplicate review tasks, or automatic full-suite reruns; Expected: one batched severity-ranked verdict; Acceptance: the reviewer reuses exact-commit implementation evidence, runs only missing/stale checks and finding reproductions, and identifies all accepted corrections in one pass; IntelliJ MCP: targeted inspect/build/test/run as evidence requires; Compose MCP: targeted state/interaction/design checks as evidence requires; Evidence: task `01a04b8b-9efe-7be0-a029-ef182814c326`, implementation commit `97e25d586c61ae0dfdae1896048d3b2a72a30dc3`, reviewed HEAD `c0700e0921b84e6079e9a2cf3dcbe3247185b825`, `docs/design/reviews/G2-state-system/IMPLEMENTATION_REVIEW.md`, verdict `CORRECTION REQUIRED`.
- [x] 4.11 Scope: only if task 4.10 finds accepted defects, run one top-level GPT-5.6 Sol High correction implementation task using reopened owning task IDs; Out of scope: repeated correction loops, root/reviewer edits, or unrelated redesign; Expected: one coherent correction commit or a recorded `NOT REQUIRED`; Acceptance: accepted findings are fixed or explicitly documented, targeted verification passes, and no final evidence is captured before the accepted commit exists; IntelliJ MCP: inspect/build/tests for changed scope; Compose MCP: affected-state checks only; Evidence: correction task `01a04ba2-9dba-7111-91a1-52606e0c225b`, correction commit `bd683366a8deb04b737652809bf7bad24571ec77`, and `docs/design/reviews/G2-state-system/IMPLEMENTATION_REVIEW.md`; stale correction-only product captures were rejected and fresh proof remains task `4.12`.
- [x] 4.12 Scope: capture the complete final G2 evidence set once after task 4.10 and any required task 4.11 correction; Out of scope: generated concepts, controller screenshots, stale captures, or duplicate pre-review evidence; Expected: screenshots, semantic trees, interaction notes under `docs/design/evidence/G2/`; Acceptance: normal happy/failure flows and forced states agree, no UI error exists, logs are clean, and all evidence names the exact accepted commit; IntelliJ MCP: confirm accepted commit still builds; Compose MCP: full required status/windows/click/type/resize/screenshots/semantics/errors/logs pass once; Evidence: complete canonical G2 evidence index.
- [x] 4.13 Scope: create the G2 review package and stop; Out of scope: G3 work; Expected: `docs/design/reviews/G2-state-system/REVIEW.md`, `EVIDENCE.md`, `manifest.json`; Acceptance: exact commit, tasks, proof, manual flow, strengths, compromises, inspection points, and approval syntax are complete; IntelliJ MCP: verify files/commit; Compose MCP: evidence references final product captures; Evidence: committed package ending `AWAITING USER: APPROVE G2 or REVISE G2: <feedback>`.

## 5. G3 Hardening And Final Design

- [ ] 5.1 Scope: after G2 approval, create a narrow Sol High dispatch packet for resilience/accessibility hardening and focused tests; Out of scope: new product features; Expected: `docs/implementation-threads/06-g3-hardening.md`; Acceptance: packet assigns tasks 5.2-5.11 from the approved G2 commit and names all proof cases; IntelliJ MCP: inspect packet; Compose MCP: packet names exact edge/keyboard/semantic checks; Evidence: packet path and dispatched task ID.
- [ ] 5.2 Scope: harden long two-line and extreme-title behavior; Out of scope: marquee, expanding window, metadata additions; Expected: media-title layout; Acceptance: title occupies at most two lines with correct ellipsis and never overlaps metadata/actions at all required sizes; IntelliJ MCP: inspect/build; Compose MCP: capture both title fixtures at minimum/default sizes; Evidence: screenshots.
- [ ] 5.3 Scope: harden long destination path and missing-thumbnail fallback; Out of scope: real path selection or remote image errors; Expected: destination/fallback components; Acceptance: path truncates with full semantics and Change reachable, fallback preserves 16:9 geometry and communicates unavailable preview; IntelliJ MCP: inspect/build; Compose MCP: capture both fixtures and semantic trees; Evidence: screenshots and semantics.
- [ ] 5.4 Scope: harden invalid URL and disabled-action states; Out of scope: backend error taxonomy; Expected: validation/disabled treatments; Acceptance: invalid text is editable and calmly explained, disabled controls remain legible and non-interactive, no status depends on color alone; IntelliJ MCP: inspect/build/tests; Compose MCP: type invalid values, attempt disabled actions, capture light/dark; Evidence: interactions and screenshots.
- [ ] 5.5 Scope: verify and correct full light/dark parity and normal Windows startup selection; Out of scope: custom palette, theme editor, or live in-session OS-theme watching; Expected: minimal theme-aware fixes only; Acceptance: no hard-coded theme-breaking color, focus/state contrast is credible, normal launch reads the current Windows preference with light fallback, restart picks up a changed preference, the review override changes content/title bar together, and every materially different status state works in both themes; IntelliJ MCP: search/inspect color use and build; Compose MCP: theme-switch all material states and capture client areas; Native Windows proof: full-window light/dark captures; Evidence: parity matrix and representative screenshots.
- [ ] 5.6 Scope: verify and correct default, minimum, and modestly larger window behavior; Out of scope: mobile/responsive redesign; Expected: layout metric fixes; Acceptance: 720×420, 620×350, and 880×560 remain aligned, stable, reachable, and free of uncontrolled expansion/clipping; IntelliJ MCP: inspect/build; Compose MCP: exact resize and screenshots for Ready plus status states; Evidence: size matrix and captures.
- [ ] 5.7 Scope: harden keyboard-only primary flow and focus restoration; Out of scope: global shortcut system; Expected: focus requesters/order fixes; Acceptance: YouTube link→format→quality→Change→primary/secondary actions follows reading order, native activation works, reset focuses the link field, and Escape does not discard work; IntelliJ MCP: inspect focus code/build/tests; Compose MCP: exercise keyboard interactions where supported and inspect semantics/focus; Evidence: keyboard checklist.
- [ ] 5.8 Scope: harden status, progress, validation, outcome, path, and fallback semantics; Out of scope: formal certification or screen-reader-specific hacks; Expected: targeted Compose semantics; Acceptance: semantic trees expose unambiguous names/roles/values, progress range, selected/disabled state, full path, and polite status changes; IntelliJ MCP: inspect semantics/build; Compose MCP: capture semantic trees for Empty, Ready, Downloading, Completed, Error; Evidence: semantic files and audit notes.
- [ ] 5.9 Scope: verify common Windows scaling and overflow reachability; Out of scope: arbitrary extreme scaling guarantees; Expected: only necessary overflow/layout fixes; Acceptance: at 125% and 150% scaling text is not clipped and essential actions remain reachable, with no scrollbar at normal default conditions; IntelliJ MCP: inspect/build; Compose MCP: capture available scale-equivalent/window tests and document any manual Windows setting step; Evidence: scaling checklist and captures.
- [ ] 5.10 Scope: finalize copy, alignment, spacing, control states, and remove nonessential UI; Out of scope: new features, decoration, animation system, token framework; Expected: bounded product composable edits; Acceptance: every visible element earns its place, labels are ordinary-user friendly, radio/combo/button hierarchy is clear, and no prohibited SaaS/IDE/mobile pattern appears; IntelliJ MCP: inspect/reformat/build; Compose MCP: final light/dark default/minimum review; Evidence: before/after critique note and final screenshots.
- [ ] 5.11 Scope: extend the smallest test set for all hardened logic and run full IntelliJ verification; Out of scope: test-framework expansion; Expected: targeted tests only; Acceptance: all tests pass, changed files have no unresolved inspection errors, `build_project` passes, and both run configurations launch at the final candidate commit; IntelliJ MCP: mandatory inspect/build/test/run; Compose MCP: not part of this task; Evidence: exact commit and verification summary.
- [ ] 5.12 Scope: after task 5.11, run one separate read-only top-level GPT-5.6 Sol High review combining technical verification with Impeccable `distill`, `harden`, `audit`, and `polish`; Out of scope: subagents, reviewer/root edits, repeated aesthetic churn, separate duplicate reviews, or scope growth; Expected: one batched severity-ranked verdict and, only when needed, at most one correction implementation re-dispatch using reopened owning task IDs; Acceptance: the reviewer reuses exact-commit implementation evidence, reruns only missing/stale checks and finding reproductions, critical/major findings are resolved or explicitly accepted, and removed elements are documented before final capture; IntelliJ MCP: targeted inspect/build/tests as evidence requires; Compose MCP: targeted affected-state checks as evidence requires; Evidence: combined final review note and correction task reference if used.
- [ ] 5.13 Scope: capture the complete final code-backed G3 evidence set once after task 5.12 and any accepted correction; Out of scope: mockups, stale captures, or duplicate pre-review evidence; Expected: `docs/design/evidence/G3/` Compose client-area screenshots, Computer Use light/dark full-window screenshots, semantic trees, interaction/size/theme matrices, and logs; Acceptance: all required edge cases, themes, sizes, keyboard flow, semantics, UI-error checks, title-bar/window behavior, and logs describe the exact accepted commit; IntelliJ MCP: confirm accepted commit/build; Compose MCP: full required tool suite once; Codex Computer Use: `Windows.Graphics.Capture` plus window-control interaction record only where Compose cannot prove native chrome; Evidence: final canonical evidence index.
- [ ] 5.14 Scope: create the G3 final review package and stop; Out of scope: real yt-dlp work or OpenSpec archive before approval; Expected: `docs/design/reviews/G3-final/REVIEW.md`, `EVIDENCE.md`, `manifest.json`; Acceptance: exact commit, tasks, proof, manual steps, strengths, compromises, 3-7 inspection points, final definition-of-done check, and approval syntax are complete; IntelliJ MCP: verify files/commit; Compose MCP: evidence references final captures; Evidence: committed package ending `AWAITING USER: APPROVE G3 or REVISE G3: <feedback>`.

## 6. Approval And Closeout

- [ ] 6.1 Scope: record `APPROVE G3` verbatim and reconcile OpenSpec with the final approved implementation; Out of scope: backend integration; Expected: G3 decision update and any accurate spec/task status corrections; Acceptance: every checked task has evidence and no unimplemented behavior is marked complete; IntelliJ MCP: final build/status spot check; Compose MCP: final no-error/status spot check; Evidence: approval commit.
- [ ] 6.2 Scope: synchronize/archive the design change using the current OpenSpec workflow; Out of scope: starting the real yt-dlp adapter change; Expected: archived/synchronized OpenSpec artifacts; Acceptance: validation passes, final specs match code, and the design change closes only after all four approvals; IntelliJ MCP: verify repository state; Compose MCP: not required after unchanged final proof; Evidence: OpenSpec archive result and final commit.
