# Implementation Thread

Model: GPT-5.6 Sol
Reasoning: High
Base commit: `f4a17c65539f233b6a5b62af62b4cd9abf901be9`
OpenSpec change: `design-primary-download-window`
Tasks: `2.2`, `2.3`
Thread type: top-level Codex task, no subagents

## Goal

Create the smallest single-module Kotlin/JVM Compose Desktop scaffold and prove the pinned Jewel standalone stack with
one launchable smoke window.

## Required context

Read these before editing:

- `D:\Downloads\INITIAL_DESIGN_ORCHESTRATOR_PROMPT.md`
- `PRODUCT.md`
- `DESIGN.md`
- `docs/design/reviews/G0-direction/REVIEW.md`
- `docs/design/reviews/G0-direction/CODABILITY.md`
- `openspec/changes/design-primary-download-window/proposal.md`
- `openspec/changes/design-primary-download-window/design.md`
- `openspec/changes/design-primary-download-window/tasks.md`
- all specs under `openspec/changes/design-primary-download-window/specs/`

Use the repository-local `openspec-apply-change` skill. Use the `impeccable` skill for product context before UI edits.
G0 approval is the confirmed shape brief; do not rerun shape or generate visual probes for this smoke-only tranche.

## In scope

- Create a minimal Gradle wrapper and single application module.
- Add only the files needed by tasks 2.2 and 2.3, expected to include:
    - `settings.gradle.kts`
    - `build.gradle.kts`
    - `gradle.properties`
    - Gradle wrapper files
    - `src/main/kotlin/downlet/Main.kt`
- Pin:
    - Kotlin `2.3.20`
    - Compose Multiplatform `1.11.0`
    - Jewel standalone `0.39.1-262.9437.29`
    - Compose Hot Reload `1.2.0`
    - Kotlin `JvmTarget.JVM_21`
    - Java `--release 21`
    - JBR `25` for Gradle development and application runtime
- Implement the smallest smoke composition that proves `IntUiTheme`, `ComponentStyling.default().decoratedWindow()`,
  Jewel `DecoratedWindow`, and a plain `TitleBar` containing only `Downlet` plus standard window controls.
- Configure a resolvable main class and launch it.
- Mark OpenSpec tasks 2.2 and 2.3 complete only after every acceptance check passes.
- Commit all assigned work with a Conventional Commit message.

## Out of scope

- Product layout, URL controls, state models, fixtures, Design Review Controller, final theme behavior, or final sizing.
- Compose Hot Reload MCP connection or `hotMcpServer` proof; task 2.9 owns that.
- Run configurations beyond any temporary IntelliJ launch needed to prove the smoke entry point; task 2.8 owns shared
  configurations.
- Tests or abstractions without logic that can regress.
- Real yt-dlp, subprocess, network, metadata, ffmpeg, filesystem writes, persistence, packaging, telemetry, updates, or
  backend seams.
- Multi-module structure, dependency injection, service interfaces, repositories, factories, custom design systems, or
  speculative scaffolding.
- Any redesign of approved G0 decisions.
- Subagents of any kind.

## UI constraints

- This is a compile-and-launch smoke window, not the G1 product surface.
- Use Jewel defaults and named parameters. Add no cards, gradients, pills, custom colors, project stripe, menu, toolbar,
  breadcrumbs, IDE actions, or decorative content.
- Keep visible content to the minimum needed to identify the smoke window.
- Do not silently substitute Material components for Jewel controls.

## Required IntelliJ MCP use

Use the IntelliJ IDEA MCP server named `intellij`, with `projectPath` on every call. Do not use WebStorm integration or
the IDE terminal.

- Inspect the project module and dependency state before editing.
- After Gradle files exist, refresh or reopen project context as needed and inspect resolved modules/dependencies.
- Inspect and reformat every changed Kotlin and Gradle Kotlin file.
- Run `build_project` after edits.
- Discover the `main` run point or a temporary IntelliJ run target and launch it.
- Verify the process uses JBR 25 while generated bytecode targets Java 21.
- If IntelliJ cannot synchronize or build, run the smallest relevant Gradle-wrapper command through the normal shell,
  then retry IntelliJ. Wrap noisy shell commands with RTK.

## Compose MCP

Not required for tasks 2.2 and 2.3. Do not claim Compose MCP evidence and do not configure a custom MCP server.

## Checks

- Gradle wrapper reports successfully.
- Dependency resolution succeeds at the pinned versions.
- Gradle build succeeds.
- IntelliJ `build_project` succeeds.
- IntelliJ launches the smoke window.
- The smoke window uses Jewel `IntUiTheme` and `DecoratedWindow` with a plain title bar.
- No unassigned product behavior or backend work appears.

If a pinned dependency or named Jewel API cannot compile, stop. Return the exact dependency/API error and evidence to
the orchestrator. Do not upgrade, downgrade, or approximate silently.

## Return contract

Return:

- commit SHA;
- changed files;
- OpenSpec task IDs completed;
- exact pinned versions;
- JVM target and JBR runtime evidence;
- IntelliJ module/dependency inspection result;
- IntelliJ inspection/reformat result;
- IntelliJ build result;
- Gradle checks performed;
- launch method and result;
- screenshots or Compose evidence produced, expected `none` for this tranche;
- known issues or `none`;
- confirmation that no subagents and no backend behavior were used.

## Dispatch record

Top-level task ID: `01a04557-3e02-7e70-b6f6-ba1fa87c5a35`
